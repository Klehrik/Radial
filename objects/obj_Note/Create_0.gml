/// obj_Note : Input

// It is currently 1:14 AM and I have gone insane.

color = c_white;
alpha = 1;

radius = 0;
radiusScale = 1;
radiusMax = 40;

seconds = 0;		// Seconds since creation ; reaching the input diamond should take a set number of beats
secondsMax = 0;

lerpX = [x, x];		// The position range of the note (depending on direction)
lerpY = [y, y];

expire = false;
hit = false;